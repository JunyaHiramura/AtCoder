# ノウハウ

## AtCoderを行う上で学んだことを蓄積する

## C++コーディング

### 関数

|関数|説明|使い方|
|-----|-----|-----|
|abs|絶対値を求める|abs(数値)|

### 連鎖代入

複数の変数に同じ値を代入するときに利用すると、簡潔に記載可能

以下のような処理
``` a = b = c = 0; ```
代入演算子は右結合であるため、
``` a = (b = (c = 0)) ```
と解釈される

### INF（Infinity）

無限大を表す
ゼロ除算やオーバーフローで発生させることができる

### std::sort()

## アルゴリズム編

### 線形探索（Linear Search）

リストや配列のデータを先頭から探していく手法