#include <bits/stdc++.h>
using namespace std;

int main() {
    string s;
    cin >> s;

    // int count = 0;
    // for (size_t i = 0; i < s.size(); i++) {
    //     for (size_t j = 0; j < s.size() - i; j++) {
    //         string tmpStr = s.substr(i, j + 1);
    //         if (tmpStr.size() == 1) {
    //             count++;
    //             continue;
    //         }
    //         bool isOk = true;
    //         for (size_t k = 0; k < tmpStr.size() - 1; k++) {
    //             if (tmpStr[k] == tmpStr[k + 1]) {
    //                 isOk = false;
    //                 break;
    //             }
    //         }
    //         if (isOk) count++;
    //     }
    // }
    
    // cout << count % 998244353 << endl;

    // return 0;
}